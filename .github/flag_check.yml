name: vulnerable-secret

on:
  issues:
    types: 
      - opened

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      password: ${{ secrets.password }}
    steps:
      - name: echo PR title
        run: echo "::add-mask::${{ github.event.issue.title }}"
